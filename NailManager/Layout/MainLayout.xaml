<UserControl x:Class="NailManager.Layout.MainLayout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="0">
            <Button Content="Login" Tag="Login" Click="NavigateBtnClick"/>
            <Button Content="Home" Tag="Home" Click="NavigateBtnClick"/>
        </StackPanel>

        <!-- Body -->
        <ContentControl x:Name="BodyContent" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0,0,0,0" Grid.Row="1"/>

        <!-- Toast Notification -->
        <Border x:Name="ToastNotification" Background="DarkGray" Height="30" VerticalAlignment="Top" Margin="0,0,0,0" Visibility="Collapsed" Grid.Row="2">
            <TextBlock Text="This is a toast notification" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Border>

        <!-- Loading Indicator -->
        <Grid x:Name="LoadingIndicator" Background="Transparent" Visibility="Collapsed" Grid.RowSpan="3">
            <Border Background="#80000000" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="20">
                <StackPanel>
                    <TextBlock Text="Loading..." Foreground="White" FontSize="20" HorizontalAlignment="Center"/>
                    <ProgressBar IsIndeterminate="True" Width="200"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
